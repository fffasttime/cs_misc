#include "simplesql.h"

void createTable(char *tableval, create_item_def *crtitem_root){
    print("trying create %s\n", tableval);
}

void selection(item_def *item_first, table_def *table_first, conditions_def *con_root){
    
}
